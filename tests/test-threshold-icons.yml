name: Test threshold icons and named report
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create coverage summary JSON
        run: |
          mkdir -p coverage
          cat > coverage/coverage-summary.json << 'EOF'
          {
            "total": {
              "lines": { "total": 100, "covered": 95, "skipped": 0, "pct": 95 },
              "statements": { "total": 100, "covered": 95, "skipped": 0, "pct": 95 },
              "functions": { "total": 20, "covered": 19, "skipped": 0, "pct": 95 },
              "branches": { "total": 40, "covered": 38, "skipped": 0, "pct": 95 }
            }
          }
          EOF

      - name: Create coverage final JSON
        run: |
          cat > coverage/coverage-final.json << 'EOF'
          {}
          EOF

      - id: action
        uses: .
        with:
          json-summary-path: coverage/coverage-summary.json
          json-final-path: coverage/coverage-final.json
          file-coverage-mode: none
          comment-on: none
          name: 'Unit Tests'
          threshold-icons: '{"0": "🔴", "80": "🟠", "90": "🟢"}'

      - name: Assert action succeeded with threshold icons
        run: |
          echo "Action completed successfully with threshold icons and named report"
          echo "PASS"
